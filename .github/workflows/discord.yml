name: Test Actions

on: 
  pull_request:
     branches: 
      - master
#   push:
#      branches: 
#       - test       
jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Discord notification
      uses: sporokiii/action-discord@master
      with:
        args: "HEHE"
    env:
        DISCORD_WEBHOOK: ${{secrets.DISCORD_WEBHOOK}}
        DISCORD_USERNAME: PROVA
        DISCORD_AVATAR: https://internetpasoapaso.com/wp-content/uploads/que-es-url.jpg
